apiVersion: 1

providers:
  - name: BakePlan dashboards
    orgId: 1
    folder: BakePlan
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
